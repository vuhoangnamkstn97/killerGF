import { ObserveSlideUpDirective } from './observe-slide-up.directive';

describe('ObserveSlideUpDirective', () => {
  it('should create an instance', () => {
    const directive = new ObserveSlideUpDirective();
    expect(directive).toBeTruthy();
  });
});
